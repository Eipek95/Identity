@{
    ViewData["Title"] = "Index";
}
@inject Microsoft.AspNetCore.Authorization.IAuthorizationService auhorizationService


@if (auhorizationService.AuthorizeAsync(User, "", "Permissions.Order.Read").Result.Succeeded)
{
    <h1>Bu buttonu Permissions.Order.Read izne sahip kullanıcılar görebilir</h1>
    <button class="btn btn-primary">Permissions.Order.Read</button>
}

@if (auhorizationService.AuthorizeAsync(User, "", "Permissions.Order.Delete").Result.Succeeded)
{
    <h1>Bu buttonu Permissions.Order.Delete izne sahip kullanıcılar görebilir</h1>
    <button class="btn btn-danger">Permissions.Order.Delete</button>
}


@if (auhorizationService.AuthorizeAsync(User, "", "Permissions.Stock.Delete").Result.Succeeded)
{
    <h1>Bu buttonu Permissions.Stock.Delete izne sahip kullanıcılar görebilir</h1>
    <button class="btn btn-success">Permissions.Stock.Delete</button>
}